<!DOCTYPE html>
<html lang="{{ lang }}">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <meta name="theme-color" content="#fff">
  <meta name="twitter:site" content="@_shoonia">
  <meta name="twitter:creator" content="@_shoonia">
  <meta name="twitter:card" content="summary">
  <meta property="og:site_name" content="{{ pkg.title }}">
  <meta property="og:url" content="{{ permalink | siteUrl }}">
  <meta property="og:type" content="article">

  {% if image %}
    <meta property="og:image" content="{{ image }}">
  {% endif %}

  {% if date %}
    <meta property="article:published_time" content="{{ date }}">
  {% endif %}

  {% if modified %}
    <meta property="article:modified_time" content="{{ modified }}">
  {% endif %}

  <meta name="description" content="{{ description if description else pkg.description }}">
  <meta property="og:description" content="{{ description if description else pkg.description }}">

  <meta property="og:title" content="{{ title if title else pkg.title }}">
  <title>{{ title if title else pkg.title }}</title>

  <link rel="preload" href="/assets/firacode@5.2.0/firacode-regular.woff2" as="font" type="font/woff2" crossorigin="anonymous">
  {{ linkPreload | safe }}
  <link rel="stylesheet" href="/assets/styles.css?v={{ enviroment.cssHash  }}">
  <link rel="manifest" href="/manifest.webmanifest">
  <link rel="icon" href="/favicon.png" type="image/png">
  <link rel="apple-touch-icon" href="/assets/icons/icon-192x192.png">
  <link rel="webmention" href="https://webmention.io/shoonia.site/webmention">
  <link rel="pingback" href="https://webmention.io/shoonia.site/xmlrpc">
  <link rel="canonical" href="{{ permalink | siteUrl }}">
  <link rel="alternate" href="{{ 'rss.xml' | siteUrl }}" type="application/rss+xml" title="{{ pkg.title }}">
  <link rel="alternate" href="{{ 'rss.json' | siteUrl }}" type="application/json" title="{{ pkg.title }}">
  <link rel="sitemap" href="/sitemap.xml" type="application/xml" title="{{ pkg.title }}">
  {{ head | safe }}
  {% if jsonLd %}
    <script type="application/ld+json">{{ jsonLd | safe }}</script>
  {% endif %}
</head>
<body>
<header>
  <nav class="content">
    <a href="/" {{ 'aria-current=page' if permalink == '/' }}>
      Home
    </a>
  </nav>
</header>

{% include 'support.njk' %}
